# Clase 6 · DPL1046 Programación en Python y Manipulación de Datos · UDLA
# Dependencias NUEVAS para los scripts 00 a 04 (de script a pipeline de producción).
#
# El curso usa un solo entorno virtual (.venv) en la raíz del repo, que se
# va completando clase a clase. pandas, numpy y pytest YA están instalados
# desde clases anteriores — NO se repiten aquí para no reinstalar de más.
#
# Instalación (con el .venv del curso ya activado):
#     pip install -r clases/clase-06/requirements.txt
#     pip freeze > requirements.txt    (actualiza el requirements.txt raíz del repo)
#
# Qué usan las librerías nuevas:
#   loguru  -> logging estructurado (scripts 01 a 04: niveles INFO/WARNING/SUCCESS,
#              log a consola y a archivo con rotación)
#   pyyaml  -> leer parámetros desde config.yaml sin tocar el código (scripts 01 y 04)
#
# Ya instaladas en clases anteriores (NO reinstalar):
#   pandas   -> Clase 1
#   numpy    -> Clase 1
#   pytest   -> Clase 3

loguru==0.7.3
pyyaml==6.0.3
schedule==1.2.2
