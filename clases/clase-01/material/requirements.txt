# DPL1046 - Programación en Python y Manipulación de Datos
# Clase 1 - Entorno base del curso
# Instalar con:  pip install -r requirements.txt

pandas==2.2.2          # manipulación de datos (motor del curso)
numpy==1.26.4          # cálculo numérico, base de pandas
openpyxl==3.1.5        # leer/escribir Excel
python-dotenv==1.0.1   # variables de entorno (.env) - seguridad desde el día 1
kagglehub==0.3.4       # descargar datasets de Kaggle de forma reproducible
boto3==1.34.131        # SDK de AWS (S3 en esta clase, más servicios después)
ydata-profiling==4.9.0 # reportes automáticos de calidad de datos (se usa desde Clase 4)